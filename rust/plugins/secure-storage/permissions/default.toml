"$schema" = "schemas/schema.json"

[default]
description = """
Default permission set for the NativeBlade secure storage plugin.
Allows reading, writing, and removing secrets in the OS keystore.
"""

permissions = [
  "allow-set-item",
  "allow-get-item",
  "allow-remove-item",
]

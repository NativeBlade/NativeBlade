"$schema" = "schemas/schema.json"

[default]
description = """
Default permission set for the NativeBlade analytics plugin.
Allows applying analytics ops (events, screens, user properties, consent).
"""

permissions = [
  "allow-apply",
]

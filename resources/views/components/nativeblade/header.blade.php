<div data-nb="header"
     data-title="{{ $title }}"
     data-back="{{ $back ? 'true' : 'false' }}"
     data-bg="{{ $bg }}"
     data-color="{{ $color }}"
     data-border-color="{{ $borderColor }}"
     style="display:none">{{ $slot }}</div>

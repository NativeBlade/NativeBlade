<div data-nb="bottom-nav"
     data-bg="{{ $bg ?? '' }}"
     data-color="{{ $color ?? '' }}"
     data-active-color="{{ $activeColor ?? '' }}"
     data-border-color="{{ $borderColor ?? '' }}"
     style="display:none">{{ $slot }}</div>

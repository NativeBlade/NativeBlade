<?php

namespace NativeBlade\Config\Media;

/**
 * @deprecated Replaced by the standard Livewire event pattern — see MEDIA.md.
 *             Trigger picks from a component via `NativeBlade::pickCamera()`,
 *             `pickGallery()`, or `pickVideo()`, and receive results with
 *             `#[On('nb:media-result')]`. This class is no longer used and
 *             will be removed in a future release.
 *
 * @internal
 */
final class IosMediaConfig
{
}

<?php

namespace NativeBlade\Plugins;

/**
 * @deprecated Replaced by the standard Livewire event pattern — see MEDIA.md.
 *             Components now receive media items as plain arrays inside the
 *             `$items` parameter on `#[On('nb:media-result')]`.
 *
 * This stub remains only because the file cannot be deleted from the current
 * sandbox. Do not use this class; it will be removed in a later release.
 *
 * @internal
 */
final class MediaItem
{
}
